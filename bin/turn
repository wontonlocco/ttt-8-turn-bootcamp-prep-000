#!/usr/bin/env ruby

require_relative '../lib/turn'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "\nWelcome to Tic Tac Toe!\n\n"

display_board(board)
index = input_to_index(user_selection)

turn(board)
move(board, index)

puts "\n\n"
