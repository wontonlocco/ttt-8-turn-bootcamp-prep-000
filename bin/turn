#!/usr/bin/env ruby

require_relative '../lib/turn'

board = [" ", " ", " ", " ", " ", " ", " ", " ", " "]

puts "\nWelcome to Tic Tac Toe!\n\n"

display_board(board)

puts "Please enter 1-9:"
  user_selection = gets.chomp!
index = input_to_index(user_selection)

turn(board, user_selection)
move(board, index)

puts "\n\n"
